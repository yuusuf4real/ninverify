# Database
DATABASE_URL="postgres://user:password@host/db"

# Auth
AUTH_SECRET="replace-with-strong-secret-minimum-32-characters"

# Paystack (Production)
PAYSTACK_PUBLIC_KEY="pk_live_..."
PAYSTACK_SECRET_KEY="sk_live_..."

# YouVerify (Production)
# IMPORTANT: YouVerify v2 API uses regular 11-digit NIN
# 
# API Endpoint: POST https://api.youverify.co/v2/api/identity/ng/nin
# Authentication: Pass token in header (not as Bearer token)
# 
# CRITICAL SETUP STEPS:
# 1. Create API key at os.youverify.co → Account Settings → API/Webhooks
# 2. Enable NIN permission when creating the key
# 3. Use LIVE environment key (not Test/Sandbox)
# 4. Fund your wallet at os.youverify.co → Billing
# 5. Ensure business account is fully onboarded (KYB complete)
#
# Common Errors:
# - 402: Insufficient wallet balance → Top up wallet
# - 403: Missing NIN permission or wrong environment → Regenerate key with NIN enabled
# - Token must be in header as "token: YOUR_KEY" (not Authorization: Bearer)
#
YOUVERIFY_TOKEN="your-live-api-token-from-os-youverify-co"
YOUVERIFY_BASE_URL="https://api.youverify.co"

# Security & Compliance
# Generate strong random string: openssl rand -base64 32
ENCRYPTION_KEY="your-encryption-key-for-sensitive-data"

# Admin System
# First super admin credentials (used by scripts/create-super-admin.ts)
FIRST_SUPER_ADMIN_EMAIL="admin@verifynin.ng"
FIRST_SUPER_ADMIN_PASSWORD="ChangeMe123!"

# Rate Limiting & Fraud Prevention
MAX_DAILY_VERIFICATIONS_PER_USER="10"
MAX_WALLET_FUNDING_AMOUNT="100000000"  # ₦1,000,000 in kobo
MIN_WALLET_FUNDING_AMOUNT="50000"      # ₦500 in kobo

# Monitoring & Logging (Optional but recommended)
SENTRY_DSN="your-sentry-dsn-for-error-tracking"
LOG_LEVEL="info"  # debug, info, warn, error

# Webhook Security (Optional - for IP whitelisting)
# ALLOWED_WEBHOOK_IPS="52.31.139.75,52.49.173.169,52.214.14.220"

# Data Retention (NDPR Compliance)
DATA_RETENTION_DAYS="365"  # How long to keep user data

# Feature Flags
ENABLE_RATE_LIMITING="true"
ENABLE_AUDIT_LOGGING="true"
ENABLE_WEBHOOK_SIGNATURE_VERIFICATION="true"
